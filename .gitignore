# Intermediate/working files generated by the PubMed research routines
# (regenerable each run; only the read_pmids_*.json state files are tracked)
esearch_*.json
efetch_*.xml
fetched_pmids_*.txt
new_pmids_*.txt
new_pmids_*.json
read_pmids_*_sorted.txt
pnbatch_*
